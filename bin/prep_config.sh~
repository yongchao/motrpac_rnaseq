#!/bin/bash -x 

##oct/5/2017:  This file is reinstated that we deployed a python script to
##obtain the sample_info.txt, which contains paired-info, lane, barcode, etc.

##The program should be run in the fastq_raw folder or fastq folder

prep_sample.py  | sort_skip.sh 1 -k3,3n -k1,1 > ../sample_info.txt
cd ..

g=hg38_gencode_decoy
if [[ "$#" -ge  1 ]]; then 
    g=$1
fi

printf "genomedir:\n    /sc/orga/projects/sealfs01a/yongchao_group_readonly/star/"$g"\n" >config.yaml

mkdir -p log/cluster

